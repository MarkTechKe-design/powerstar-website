# Powerstar Supermarkets - .htaccess Configuration
# Generated for cPanel/Apache deployment

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # 1. Force HTTPS
    # Redirects all http traffic to https
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # 2. Remove .html Extensions (Clean URLs)
    # If the request is not a directory
    RewriteCond %{REQUEST_FILENAME} !-d
    # If the request is not a file (it's a clean URL like /about)
    RewriteCond %{REQUEST_FILENAME} !-f
    # But a file with .html exists (like /about.html)
    RewriteCond %{REQUEST_FILENAME}.html -f
    # Internally rewrite to the .html file
    RewriteRule ^([^\.]+)$ $1.html [NC,L]
</IfModule>

# 3. Custom 404 Error Page
ErrorDocument 404 /404.html

# 4. Browser Caching (Speed Optimization)
<IfModule mod_expires.c>
    ExpiresActive On
    
    # Images
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"

    # CSS & JavaScript
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"

    # Default
    ExpiresDefault "access plus 2 days"
</IfModule>
